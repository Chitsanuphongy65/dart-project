void main() {
  // สร้าง List สำหรับเก็บชื่อ
  List<String> names = ['Alice', 'Bob', 'Charlie', 'Diana'];

  // แสดงผลชื่อทั้งหมดใน List
  for (int i=0; i<names.length; i++) {
    print(names[i]);
  }
}
